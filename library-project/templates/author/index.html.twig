{% extends 'base.html.twig' %}

{% block title %}Liste des Auteurs{% endblock %}

{% block body %}
<div class="container">
    <h1 class="my-4">Nos Auteurs</h1>
    
    <div class="row row-cols-1 row-cols-md-3 g-4">
        {% for author in authors %}
            <div class="col">
                <div class="card h-100">
                    <div class="card-body">
                        <h5 class="card-title">{{ author.name }}</h5>
                        <p class="card-text">
                            Livres :
                            {% for book in author.books %}
                                {{ book.title }}{% if not loop.last %}, {% endif %}
                            {% endfor %}
                        </p>
                    </div>
                    <div class="card-footer">
                        <a href="{{ path('author_detail', {id: author.id}) }}" class="btn btn-primary">Détails</a>
                    </div>
                </div>
            </div>
        {% else %}
            <div class="col-12">
                <div class="alert alert-info">Aucun auteur trouvé.</div>
            </div>
        {% endfor %}
    </div>
</div>
{% endblock %}